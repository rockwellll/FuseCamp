<%= form_with(model: t1, local: true) do |form| %>
  <% if t1.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(t1.errors.count, "error") %> prohibited this t1 from being saved:</h2>

      <ul>
        <% t1.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
